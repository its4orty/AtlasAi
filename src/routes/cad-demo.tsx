import { createFileRoute } from "@tanstack/react-router";
import { demoDimensionedCadModel, renderCadSvg, renderDxf } from "~/cad";
export const Route=createFileRoute("/cad-demo")({ server:{ handlers:{ GET:async()=>{const m=demoDimensionedCadModel(); const svg=renderCadSvg(m); const dxf=renderDxf(m); const html=`<!doctype html><title>ATLAS AI Accurate CAD demo</title><style>body{font:16px system-ui;max-width:1000px;margin:2rem auto;color:#172033}svg{width:100%;border:1px solid #ccd6df;background:#fff}a{display:inline-block;padding:.7rem 1rem;background:#172033;color:white;text-decoration:none;border-radius:5px}</style><h1>Accurate CAD — demo</h1><p><strong>Demo — synthetic data, not a survey.</strong> This is a Phase 1 rendering preview.</p>${svg}<p><a download="atlas-cad-demo.dxf" href="data:application/dxf;charset=utf-8,${encodeURIComponent(dxf)}">Download DXF</a></p>`; return new Response(html,{headers:{"content-type":"text/html; charset=utf-8"}});}}}});
